import '../lib';
import { Scale } from '../lib/scale.js';

/**

  UI wrapper to Chord.chordsFromScale

  Take note names from user,
  Convert to scale,
  Get the scale chords (up to some degree parameter),
  Display using VexFlow / Lilypond / MuseScore / MIDI

**/
